import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class SwUpdateService {
  init(): void {
    // Intentionally left as a shell service to preserve existing app initializer wiring.
  }
}
